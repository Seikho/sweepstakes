module.exports = function isLoggedIn(target, propertyKey, descriptor) {
    var method = descriptor.value;
    // sessions.isValid(token)
    // 	.then(isValid => {
    // 	});
};
//# sourceMappingURL=isLoggedIn.js.map